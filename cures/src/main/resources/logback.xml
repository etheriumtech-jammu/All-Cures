<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="true" scanPeriod="15 seconds">
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>
<!-- Wallet error rolling file appender -->
    <appender name="WALLET_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!-- Relative to the JVM working directory -->
        <file>logs/wallet-errors.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!-- Daily rollover, gzipped -->
            <fileNamePattern>logs/wallet-errors.%d{yyyy-MM-dd}.log.gz</fileNamePattern>
            <!-- Keep 30 days -->
            <maxHistory>30</maxHistory>
        </rollingPolicy>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Dedicated logger for wallet code: only writes to the wallet file -->
    <logger name="wallet" level="ERROR" additivity="false">
        <appender-ref ref="WALLET_FILE"/>
    </logger>
  
    <root level="ERROR">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
